<%= form_with(model: api_v1_account, local: true) do |form| %>
  <% if api_v1_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_account.errors.count, "error") %> prohibited this api_v1_account from being saved:</h2>

      <ul>
        <% api_v1_account.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
